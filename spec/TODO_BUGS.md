Edit mode on blue (default) theme
- white text on black background. Annoying.

Auto textwrap on end of line will put user on the blank space on next line instead of inserting line!
If next line isn't blank, a new line is inserted.

After typing a file say, spec/hello.txt Save after selecting a folder, say spec fails if spec/spec folder doesn't exist.
Also this is just confusing.