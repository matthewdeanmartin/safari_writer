[tox]
envlist = py310, py311, py312, py313
isolated_build = True

[testenv]
deps =
    pytest>=8.0.0
    hypothesis>=6.151.9
    textual>=1.0.0
    pyenchant>=3.2.0
    pytest-xdist>=3.5.0
commands =
    pytest tests/ -v -n 8 -q
