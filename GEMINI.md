# Gemini CLI

Shared project instructions live in @AGENTS.md.

## Mandates

- **Ignore the `images/` folder.** Do not run search tools (e.g., `grep_search`) against this directory, and do not attempt to include its contents in the context.
