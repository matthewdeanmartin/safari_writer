- Default file name document.txt when there should be no default!
- Should default to .swf extension if none provided
- Should warn and get confirmation if a file with format codes is saved to anything other than .swf
- Should not prompt for directory when saving file again. We're not continually changing the folder!
- "Save as..." is not implemented and is not the default behavior!
---
file selector
- arrow down and up works, but page up and page down doesn't  should skip 5 at a time.
---
Create file
- should warn/prompt if there already is a file and get a confirmation before 
- should tell the user if there are changes, but still ask the user if they really want to create a new file
- if no file in focus, then create file with no prompt
- should save unnamed file
- should load unnamed file if abruptly interrupted so we don't have data loss
----
Menu
- main menu needs a Q for quit.
- all submenus need R (return to main menu), just like proof reader has.
---
Undo
- We need an undo feature. Did atari writer really not have undo?!
---
Main menu
- Should be two columns with Index 1/2 being the start of the 2nd column ()